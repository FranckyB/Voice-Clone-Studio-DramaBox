# Voice Clone Studio - Windows Runtime Requirements
# Build: DramaBox + Qwen3-TTS VoiceDesign + MMAudio + ASR + shared utilities.
# Legacy engines removed: LuxTTS, Chatterbox, Fish Speech.

# Core audio/model runtime dependencies (all platforms)
numpy==1.26.4
librosa==0.11.0
torchaudio==2.9.1+cu130
soundfile==0.13.1
mutagen==1.47.0
sox==1.5.0
einops==0.8.2
huggingface_hub==0.36.2
onnxruntime==1.24.4
onnxruntime-gpu==1.24.4; sys_platform != 'darwin'
hf_xet
# NOTE: install with --no-deps to avoid onnxruntime conflicts
qwen-tts==0.1.1
# NOTE: downgrades transformers to 4.57.3 (qwen-asr declares 4.57.6 but works fine with 4.57.3)
faster-qwen3-tts==0.2.6

# Web UI and utilities
gradio==6.7.0
gradio_filelister @ file:wheel/gradio_filelister-0.4.0-py3-none-any.whl
diffusers==0.37.1
markdown==3.10.2
tensorboard==2.20.0

# MMAudio Sound Effects dependencies (torchvision installed with PyTorch in setup script)
open_clip_torch==3.3.0
torchdiffeq==0.2.5
timm==1.0.26
colorlog==6.10.1
omegaconf==2.3.0
av==17.0.0

triton-windows==3.6.0.post26; sys_platform == 'win32'
ninja==1.13.0

# 4-bit quantization support (used by DramaBox/Gemma paths)
bitsandbytes==0.49.2

# DramaBox finetune/runtime shared deps
accelerate==1.12.0
safetensors==0.7.0
sentencepiece==0.2.1
PyYAML==6.0.3
pydantic==2.10.6
peft>0.19.0